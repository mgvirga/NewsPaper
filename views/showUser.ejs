<html class="cool">
    <header>
        <%- include('navigationHeader') -%>
        <link rel="stylesheet" href="/css/bootstrap.min.css" />
    </header>
    <head>
        <h1>Users</h1>
        <style>
            body {
            background-color: white;
            color: black;
            padding: 5px;
            }
            table{
            border: 1px solid black;
            padding: 3px;
            width: 100%;
            }
            td, th {
            border: 1px solid black;
            padding: 3px;
            columns: 150px;
            }
            /* .cool{ background-image: linear-gradient(white, blue) !important;}
            table 
            {
                font-family: arial, sans-serif;
                border-collapse: collapse;
                width: 100%;
            }
            td, th 
            {
                border: 5px solid black;
                text-align: left;
                padding: 20px;
            } */
        </style>
    </head>
<body>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Username</th>
                <th>Edit/Delete</th>
                
            </tr>
        </thead>
        <tbody>
            <% posts.forEach(function(post){ %>
                <tr>
                    <td><%=post._id%></td>
                    <td><%=post.name%></td>
                    <td><%=post.username%></td>
                    <td>
                        <button name="edit" onclick="location.href='/editUser/<%= post._id %>'">Edit</button>
                        <button name="delete" onclick="deleteAlert('/showUser/delete/<%= post._id %>')">Delete</button>
                
                        <!-- Danielle added this script function for popup and delete 4/23 -->
                <script>
                    function deleteAlert(url)
                    {
                        var c = confirm("Do you want to delete news?")
                        if (c) {
                            return location.href=url;
                        }
                    }
               </script>
                        <!-- Danielle old delete Script
                <script>
        
                    function deleteAlert()
                    {
                        if (confirm("Do you want to delete user?") == true) {

                            location.href='/showUser/delete/<%= post._id %>'
                        }
                    }
               </script> -->
                    </td>
                </tr>
            <%});%>
        </tbody>
    </table>
    <script>
        $(document).ready(function () {
            $('#display').DataTable();
        });
    </script>
</body>
<!-- Danielle commented code -->
<!-- <style>
    html, body
    {
      height: 100%;
      background-image: linear-gradient(yellow, deepskyblue);

      /* Full height */
      height: 100%;

      /* Center and scale the image nicely */
      background-position: center;
      background-repeat: no-repeat;
      background-size: cover;
    }
  </style> -->
   <footer>
    <%- include('chat') -%>
</footer>
</html>